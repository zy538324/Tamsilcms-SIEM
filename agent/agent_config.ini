# Agent configuration file
# This file defines all connection and identity settings for the agent.
# Example values are provided. Edit as needed.

[agent]
transport_url = "https://10.252.0.1:8085"
tenant_id = "" # Leave blank to auto-detect from the system.
asset_id = "" # Leave blank to use the hostname.
identity_id = "" # Leave blank to auto-detect from the system.
agent_version = "0.1.0"
hostname = "" # Leave blank to auto-detect.
os_name = "" # Leave blank to auto-detect.
trust_state = "bootstrap"
shared_key = "" # Prefer AGENT_HMAC_SHARED_KEY from env/agent.env.
cert_fingerprint = "sha256:placeholder"
identity_header = "agent-placeholder"
heartbeat_interval_seconds = 45
watchdog_timeout_seconds = 120
max_heartbeat_interval_seconds = 300
expected_binary_hash = ""
