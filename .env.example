# ============================================================================
# CRITICAL SECURITY NOTICE
# ============================================================================
GENERATE_CERT_FINGERPRINT_sha256# 
# Setup Instructions:
# 1. Copy this file to .env in the same directory
#    cp .env.example .env
# 2. Edit .env with your actual credentials
# 3. NEVER commit .env to git (.gitignore blocks it)
# 4. Generate secure random values for keys/secrets (see below)
# ============================================================================

# Root example .env for local development
# Copy to .env and edit values for your environment.

# Identity service
IDENTITY_HMAC_SHARED_KEY=GENERATE_SECURE_KEY_HERE_USE_openssl_rand_hex_32
IDENTITY_SIGNATURE_TTL=120
IDENTITY_PORT=8082

# Transport service
TRANSPORT_TRUSTED_FINGERPRINTS=sha256:GENERATE_SECURE_KEY_HERE_USE_openssl_rand_hex_32
TRANSPORT_PORT=8081

# Ingestion service
INGESTION_DATABASE_DSN=postgresql://username:password@10.252.0.5:5432/tamsilcmssiem?sslmode=require
INGESTION_PORT=8000

# Service Host Configuration (for running services on specific IP)
# By default, services bind to 127.0.0.1 (localhost only)
# To bind to 10.252.0.5, set these environment variables:
# Example: export IDENTITY_HOST=10.252.0.5
# IDENTITY_HOST=10.252.0.5
# TRANSPORT_HOST=10.252.0.5
# INGESTION_HOST=10.252.0.5
# PENETRATION_HOST=10.252.0.5
# PATCH_HOST=10.252.0.5

# =============================================================================
# HOW TO GENERATE SECURE KEYS
# =============================================================================
# For IDENTITY_HMAC_SHARED_KEY and other secrets, use:
#   openssl rand -hex 32
# 
# For certificate fingerprints:
#   openssl x509 -in cert.pem -noout -fingerprint -sha256
# 
# IMPORTANT: Use different keys for production vs development!
# =============================================================================
