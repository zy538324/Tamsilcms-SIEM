# Data Model

Scope: Canonical schemas and shared domain models.

Responsibilities:
- Define canonical identifiers (tenant_id, asset_id, identity_id, event_id).
- Provide immutable event ledger schema (future MVPs).

